{% extends 'layout_page.twig' %}

{% set page = 'stats' %}

{% block title %}GitList{% endblock %}

{% block content %}
    {% include 'breadcrumb.twig' with {breadcrumbs: [{dir: 'Statistics', path:''}]} %}

    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th width="30%">File extensions ({{ stats.extensions|length }})</th>
                <th width="40%">Authors ({{ authors|length }})</th>
                <th width="30%">Other</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>
                    <ul>
                    {% for ext, amount in stats.extensions %}
                        <li><strong>{{ ext }}</strong>: {{ amount }} {{ amount > 1 ? 'files' : 'file' }} </li>
                    {% endfor %}
                    </ul>
                </td>
                <td>
                    <ul>
                    {% for author in authors %}
                        <li><strong><a href="{{ app.request.basepath }}/{{repo}}/contributor/{{ author.email }}">{{ author.name }}</a></strong>: {{ author.commits }} {{ author.commits > 1 ? 'commits' : 'commit'}}</li>
                    {% endfor %}
                    </ul>
                </td>
                <td>
                    <p>
                        <strong>Total files:</strong> {{ stats.files }}
                    </p>

                    <p>
                        <strong>Total bytes:</strong> {{ stats.size }} bytes ({{ ((stats.size / 1024) / 1024) | number_format }} MB)
                    </p>
                    {#
                    {% for statistic in prettyStats.statistics %}
                    <p>
                        <strong>{{ statistic.title }}:</strong> {{ statistic.value }}
                    </p>
                    {% endfor %}
                    #}
                </td>
            </tr>
        </tbody>
    </table>
{#
    <hr>
    <h2>Commits by date</h2>
    <div id="commitsByDate"></div>
    <script>
        var commitsByDate = {{ prettyStats.charts.date|json_encode(constant('JSON_PRETTY_PRINT'))|raw }}
    </script>

    <hr>
    <h2>Commits by hour</h2>
    <div id="commitsByHour"></div>
    <script>
        var commitsByHour = {{ prettyStats.charts.hour|json_encode(constant('JSON_PRETTY_PRINT'))|raw }}
    </script>
#}
    <hr>
    <h2>Commits by day</h2>
    <div id="commitsByDay"></div>
    <script>
        var commitsByDay = {{ prettyStats.charts.day|json_encode(constant('JSON_PRETTY_PRINT'))|raw }}
    </script>

    <hr />

{% endblock %}

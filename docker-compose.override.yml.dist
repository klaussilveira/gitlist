volumes:
  var-cache-volume:
  var-dart-sass-volume:

services:
  webserver:
    volumes:
      - .:/application:cached
    ports:
      - '8880:80'

  php-fpm:
    volumes:
      - /var/www:/var/repositories
      - .:/application:cached
      - var-cache-volume:/application/var/cache
      - var-dart-sass-volume:/application/var/dart-sass
      - $HOME/.composer:/root/.composer/:cached
